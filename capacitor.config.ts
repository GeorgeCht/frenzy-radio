import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frenzyradio.app',
  appName: 'Frenzy Radio',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
